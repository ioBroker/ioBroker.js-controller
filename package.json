{
  "name": "iobroker.js-controller",
  "private": true,
  "dependencies": {
    "@iobroker/controller-core": "file:packages/core"
  },
  "devDependencies": {
    "@alcalzone/release-script": "^2.0.0",
    "@types/fs-extra": "^9.0.11",
    "@types/node": "^15.12.4",
    "eslint": "^7.29.0",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "prerelease": "lerna bootstrap && release-script --lerna-check",
    "release": "lerna version --no-commit-hooks",
    "version": "release-script --lerna",
    "postversion": "git push && git push --tags",
    "lint": "eslint \"packages/*/*.js\" \"packages/*/lib/**/*.js\"",
    "test": "echo \"no tests defined\""
  }
}