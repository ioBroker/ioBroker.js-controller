{
  // own json config, for test files root is different, just using ts-node no build
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "."
  },
  "include": [
    "**/*.ts"
  ],
  "exclude": ["*"]
}
